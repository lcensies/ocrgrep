[project]
name = "ocrgrep"
version = "0.1.0"
description = "Parallel OCR grep over images with dedup and checkpointing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tesserocr",
    "Pillow",
    "tqdm",
]

[project.scripts]
ocrgrep = "ocr_grep:main"
